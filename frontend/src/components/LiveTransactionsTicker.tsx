"use client";
import React from 'react';

// Stubbed no-op ticker component to satisfy legacy imports without rendering anything.
// This can be removed once all pages stop importing it.
export default function LiveTransactionsTicker() {
  return null;
}
// File intentionally removed. Keeping a 0-byte-safe module stub would be ideal, but this repository requires a valid TSX export when present.
// This file is about to be deleted at the filesystem level. If you see this comment, a deletion failed.
export {}
